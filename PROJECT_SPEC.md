# 海上测量船遥感载荷试验窗口编排服务

## 项目目标

服务以“申请接入—快照校验—资源预留—遥测执行—批次固化”为唯一主线。Go 后端通过 HTTP 提供申请、快照、批次和遥测接口，以 SQLite WAL 保存权威状态、幂等记录和顺序事件；浏览器操作页用于提交试验申请并查询资源、设备状态和事件时间线。计划包含 26 个生产 Go 文件、约 2490 行有效生产代码，合理范围为 2400—2650 行；前端、测试和生成产物不计入该规模。代码分布于 api、constraints、reservation、domain、execution、telemetry、persistence 等职责明确的包，规模来自约束插值、并发事务、状态机、消息去重和重启恢复。

## 端到端业务流程

1. 快照登记与申请接入：操作员登记带来源、有效期和摘要的轨道及海况快照，再提交包含半开时间窗、姿态轨迹、海况阈值、设备用量和关键级别的申请；服务以 Idempotency-Key 固定首次处理结果。
2. 约束判定：约束校验器在申请轨迹、轨道能力包络和海况采样点的联合时间轴上进行线性插值，检查覆盖范围、姿态角、角速度、浪高、风速、升沉和快照有效期，生成可追溯的逐项判定。
3. 并发预留：校验通过后，资源预留器在写事务中锁定判定视图，按半开区间检查各设备容量；成功时同时写入预留、试验批次、状态版本和事件，冲突时保存明确的拒绝原因且不留下占用。
4. 遥测驱动执行：生产调度器按时钟推进已预留批次，遥测接收器接收设备心跳并去重；可选设备超时使批次进入 DEGRADED，恢复后回到 RUNNING，关键设备持续失联或错过启动窗口则进入 ABORTED。
5. 终止与恢复：到达窗口结束边界时，已启动批次固化为 COMPLETED，并生成包含申请摘要、快照摘要、预留集合、已接收遥测和终止事件的不可变清单；进程重启后从数据库验证事件序列、重建投影并重新推导未完成批次的定时动作。

## 核心组件与职责

1. 申请 API：负责轨道与海况快照登记、申请接收、幂等响应、批次查询、事件游标查询，以及嵌入式浏览器操作页的静态资源和 JSON 接口。
2. 约束校验器：对不可变快照做摘要、覆盖区间、采样间隔、姿态包络、角速度和海况阈值校验，输出稳定排序的判定明细。
3. 资源预留器：维护设备容量视图，在 SQLite 写事务内完成重叠检测、容量汇总、并发争用处理及失败回滚。
4. 执行状态机：集中定义合法迁移、窗口边界、启动宽限、设备降级、故障终止、完成优先级和基于注入时钟的定时推进。
5. 遥测接收器：校验批次与设备归属，以消息摘要和设备序号处理重复、篡改及乱序心跳，并在单一事务中更新存活状态和触发状态机。
6. 持久化与事件恢复模块：提供迁移、事务仓储、幂等表、顺序事件流、查询投影、终态清单以及启动时的校验、重放和定时任务恢复。

## 领域规则与不变量

1. 所有业务时间统一规范为 UTC 微秒，试验窗、快照覆盖和资源预留均使用半开区间 [start,end)；start 必须早于 end，时长限制为 2—120 分钟，相邻窗口不冲突。
2. 轨道与海况快照一经登记不可修改，保存规范化 JSON 的 SHA-256、来源时间、有效区间和单位；申请区间必须完全落入两类快照的有效区间，校验过程禁止外推。
3. 姿态申请由按时间排序的横滚角、俯仰角采样以及最大角速度组成；校验器在申请点和轨道包络变化点的联合时间轴上插值，要求每一点都落入可行包络，连续点推导出的角速度不得超过申请与轨道上限中的较小值。
4. 海况校验在申请边界及海况采样点上插值显著波高、风速和升沉；数值等于申请阈值时通过，超过阈值或采样间隔大于快照声明上限时拒绝。
5. 每项设备需求包含 resource_id、quantity、critical 和 timeout；重叠关系满足 max(startA,startB) < min(endA,endB)，任意重叠时段的已预留数量与新需求之和不得超过设备容量。
6. 有效申请的持久化、判定结果、资源预留、批次创建、初始事件和幂等响应必须处于同一事务；冲突拒绝可被持久化，但事务错误必须回滚全部写入。
7. 同一 Idempotency-Key 与相同规范请求摘要返回首次 HTTP 状态码和响应体；同键不同摘要返回 409，且不得创建第二份申请、批次或事件。
8. 状态仅允许 RECEIVED→VALIDATED/REJECTED、VALIDATED→RESERVED/REJECTED、RESERVED→RUNNING/DEGRADED/ABORTED、RUNNING→DEGRADED/COMPLETED/ABORTED、DEGRADED→RUNNING/COMPLETED/ABORTED；终态不可迁移。
9. 心跳以 message_id 和请求体摘要判定重放：同标识同摘要返回首次结果，同标识不同摘要返回 409；新标识但设备序号不大于最后接受序号时返回 422，且不改变设备或批次状态。
10. 设备活性使用服务端 received_at 计算，等于 timeout 时仍视为在线，只有差值严格大于 timeout 才失联；可选设备失联触发降级，关键设备失联触发终止。
11. 同一时间点同时满足窗口结束和设备超时时，窗口结束判定优先；已启动批次进入 COMPLETED，未能在窗口内启动的批次进入 ABORTED。
12. 每个批次事件在事务内分配从 1 开始且连续递增的 aggregate_seq；终态清单采用规范字段顺序和摘要，记录申请、快照、预留、遥测消息及末事件序号，生成后不可修改。

## 数据模型与持久化

1. Application：申请标识、幂等摘要、时间窗、姿态采样、海况阈值、设备需求、当前状态、状态版本、创建时间和拒绝原因。
2. OrbitSnapshot：来源元数据、有效区间、轨道能力采样、横滚与俯仰包络、最大跟踪角速度、规范化摘要和登记版本。
3. SeaSnapshot：来源元数据、有效区间、浪高、风速、升沉采样、最大允许采样间隔、规范化摘要和登记版本。
4. ResourceSpec 与 Reservation：设备容量和单位，以及批次、半开预留区间、数量、关键级别、超时参数和释放状态。
5. TrialBatch：申请引用、两类快照摘要、执行状态、版本、启动与终止时间、终止原因、最后事件序号和终态清单摘要。
6. TelemetryInbox 与 DeviceLiveness：消息标识、消息摘要、设备序号、观测时间、接收时间、首次处理结果，以及每个批次设备的最后有效心跳。
7. AuditEvent：批次或申请聚合标识、aggregate_seq、事件类型、规范化载荷、服务端时间和前序事件摘要。
8. IdempotencyRecord 与 RecoveryCheckpoint：键、请求摘要、首次响应，以及投影版本、已重放事件序号和最近一次恢复结果。

## 公开接口

1. POST /api/v1/orbit-snapshots 与 POST /api/v1/sea-snapshots 登记不可变快照；返回快照标识、规范化摘要和有效区间，格式、单位或摘要不合法时返回 422。
2. POST /api/v1/applications 接收申请并要求 Idempotency-Key；成功预留返回 201 和 batch_id，约束不通过返回带稳定原因码的 422，资源冲突返回带冲突设备和区间的 409。
3. GET /api/v1/applications/{id}、GET /api/v1/batches/{id} 与 GET /api/v1/batches/{id}/events?after_seq=N 提供状态、逐项判定、设备活性、预留和连续事件查询。
4. POST /api/v1/batches/{id}/telemetry/heartbeats 接收 message_id、resource_id、device_seq、observed_at 和指标；响应包含 accepted、replayed、batch_state 与 event_seq。
5. GET /health/live 与 GET /health/ready 区分进程存活和数据库迁移、事件校验、恢复完成状态；恢复发现摘要损坏或序列缺口时 readiness 保持失败。
6. 根路径提供真实浏览器操作页，可登记或选择快照、填写姿态与设备需求、提交申请，并轮询批次及事件接口展示执行时间线和设备状态。前端使用 TypeScript 与固定版本构建工具，提交 web/package.json、web/package-lock.json 和源码；npm ci 后执行 npm run build，将确定性产物输出到 Go 可嵌入目录。
7. 生产环境使用周期调度器和真实时钟；公开测试通过相同服务层注入 ManualClock，并由 tests/telemetry_sim_test.go 的脚本模拟器执行 Send、Duplicate、OutOfOrder、Drop、Advance 和 Concurrent 指令，不依赖休眠或外部网络。

## 失败边界

1. 请求结构、单位或时间格式错误在事务前拒绝；已经进入领域判定的失败会保存稳定原因码、判定明细和拒绝事件。
2. 快照摘要不匹配、覆盖不足、采样缺口或插值越界使申请拒绝，既不创建批次也不尝试设备预留。
3. 并发写入由 SQLite 写事务串行化；数据库繁忙只进行固定次数、无随机抖动的短重试，耗尽后返回 503，幂等键仍可安全重试。
4. 预留检查后的任意插入、事件写入或提交错误都会回滚；测试可通过仓储接口注入指定检查点错误，以验证不存在孤立预留或半成批次。
5. 重复遥测返回首次结果；摘要冲突、未知设备、终态批次或倒退序号不会刷新设备活性，也不会追加状态事件。
6. 调度器并发触发同一批次时依赖状态版本比较更新，只有一个事务可以产生迁移事件；失败任务从持久化状态重新推导，而非依赖内存队列。
7. 进程退出不会丢失已提交事件；启动恢复按 aggregate_seq 重放并验证前序摘要，序列缺口或终态清单不一致时停止写服务并暴露明确的就绪错误。
8. 浏览器页对 409、422、503 和遥测拒绝展示服务端原因码，轮询中断后可依据 last event_seq 继续查询，不会重复呈现已确认事件。

## 验收标准

1. 使用仓库内固定快照从浏览器页提交申请后，可查询到完整状态时间线；所有合法迁移成功，非法迁移和终态后迁移均被拒绝且不新增事件，前端能够呈现相同结果。
2. 对同一容量设备并发提交重叠申请时，最终最多一个批次持有超出余量的那部分资源，另一申请得到稳定冲突结果；相邻半开窗口可以同时预留。
3. 相同申请或心跳重放任意次数只产生一份业务记录和一次状态影响；同一幂等标识携带不同内容时返回冲突且权威状态不变。
4. 开始、结束、快照有效期、插值阈值和设备 timeout 的等值边界符合已定义规则；窗口结束与失联同时发生时得到唯一且可复现的终态。
5. 可选设备失联使运行批次降级并可由新心跳恢复；关键设备超过宽限或批次错过启动窗口时只产生一次 ABORTED 迁移。
6. 在预留、遥测和状态迁移事务的每个注入故障点执行回滚后，申请、预留、批次版本、设备活性、幂等响应与事件数量保持一致，不存在部分提交。
7. 任何成功操作产生的事件序号连续、无重复且载荷摘要稳定；并发遥测完成后，按 aggregate_seq 读取的事件顺序与最终批次投影一致。
8. 复制同一持久化文件并在相同 ManualClock 时刻重启两次，恢复后的状态、定时推进、事件和终态摘要完全一致；干净检出执行 npm ci、npm run build、go test ./... 与 go build ./... 均成功，重复前端构建得到相同资产摘要。

## 确定性测试场景

1. tests/constraints_test.go 至少包含 5 例：D01 半开窗口覆盖与相邻边界；D02 联合时间轴插值恰好等于姿态包络；D03 连续姿态点角速度超限；海况阈值等值通过与微小超限拒绝；快照缺口禁止外推。
2. tests/reservation_test.go 至少包含 4 例：D04 多条重叠预留的容量累计；相邻窗口互不冲突；并发申请最终仅一个获得剩余容量；D05 在预留插入后注入提交错误并验证无孤立记录。
3. tests/execution_test.go 至少包含 6 例：D06 非法状态迁移无副作用；完整 RESERVED→RUNNING→COMPLETED 路径；RESERVED 直接以缺少可选设备进入 DEGRADED；可选设备丢失与恢复；D08 timeout 等值仍在线而越界后关键设备终止；结束边界优先于同刻失联。
4. tests/telemetry_test.go 至少包含 4 例：D07 相同 message_id 与摘要重放；同标识不同摘要冲突；新标识携带倒退 device_seq 被拒绝；两个并发副本只刷新一次活性并产生一次事件。
5. tests/recovery_test.go 至少包含 4 例：D09 投影落后时按连续事件重建；在 RESERVED、RUNNING、DEGRADED 三种状态分别重启并推导相同结果；事件序列缺口使 readiness 失败；终态清单摘要在多次重启后保持不变。
6. tests/api_web_test.go 至少包含 4 例：D10 相同 Idempotency-Key 和请求返回逐字节一致响应、同键异请求返回 409；约束拒绝与资源冲突的 HTTP 映射；页面静态资源可加载并能调用申请及批次接口；锁文件安装和两次前端构建的资产清单一致。
7. tests/telemetry_sim_test.go 提供共享 ManualClock、HTTP 记录器、并发屏障和脚本式遥测模拟器；所有测试使用临时 SQLite 文件、固定 ID 序列和仓库内冻结样例，不使用真实睡眠、当前时间或在线数据。
8. 公开样例位于 testdata/public：一份由公开发布 TLE 固化得到的轨道通过快照和一份公开海洋浮标历史切片，连同来源说明、单位、规范化 JSON 与 SHA-256 一并提交，确保离线测试可复现。十个 D01—D10 断言点分别落在时间区间、插值、姿态速率、容量、事务回滚、迁移表、消息摘要、超时、事件恢复和请求幂等逻辑上，可独立定位和验证缺陷。

## 组件追踪关系

1. 申请 API → cmd/server 2 个文件与 internal/api 4 个文件，目标约 430 行；覆盖快照登记、申请接入、查询、幂等响应、浏览器资源嵌入及验收 1、3、8。
2. 约束校验器 → internal/constraints 3 个文件，目标约 320 行；覆盖轨道与海况联合插值、覆盖检查、判定明细及 D01—D03。
3. 资源预留器 → internal/reservation 3 个文件，目标约 270 行；覆盖容量累计、并发写事务、冲突结果、回滚及 D04—D05。
4. 执行状态机 → internal/domain 3 个文件与 internal/execution 4 个文件，目标约 700 行；覆盖领域类型、迁移表、调度、边界优先级、降级恢复和故障终止及 D06、D08。
5. 遥测接收器 → internal/telemetry 2 个文件，目标约 250 行；覆盖消息摘要、设备序号、活性更新、并发去重及 D07。
6. 持久化与事件恢复模块 → internal/persistence 5 个文件，目标约 520 行；覆盖 SQLite 迁移、事务仓储、顺序事件、投影、终态清单与重启恢复及 D05、D09、D10。六项合计 26 个生产 Go 文件、约 2490 行，不依赖占位声明或闲置包。

## 独特性

项目把海上测量船的轨道可用性、动态海况、载荷姿态包络、船载设备容量和遥测存活度收束到同一条可恢复试验批次链中，核心难点是跨时间快照插值、并发资源承诺与事件驱动执行之间的确定性一致。
