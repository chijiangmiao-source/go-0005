# Public Offline Snapshot Sources

The orbit fixture is a frozen derivative of publicly distributed CelesTrak TLE
data transformed into service envelope samples. The sea fixture is a frozen
slice modeled after public NOAA buoy history units. Tests use these checked-in
JSON files and do not contact external services.

SHA-256 file checksums are recorded in SHA256SUMS.
